#include <iostream>
#include <algorithm>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int bispos=0, X=0 ,Y=0;

    map<pair<int,int>int> tabuleiro;

    cin >> bispos;

    for (int i=1;i<=bispos;i++){
        if (cin >> X >> Y){
            tabuleiro[{X,Y}] = 1
        }
    }

    for (auto item:tabuleiro){
        
    }
        

    return 0;
}
